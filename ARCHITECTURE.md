# 架构与工作流 (Architecture & Workflow)

## 系统架构图

```
┌────────────────────────────────────────────────────────────────┐
│                     浏览器 (Browser)                             │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │            React App (Vite)                               │ │
│  │   App.tsx → Pages (Auth, Library, Practice, etc.)        │ │
│  └────────────────┬────────────────────────────────────────┘ │
│                   │ Component Logic                           │
│  ┌────────────────▼────────────────────────────────────────┐ │
│  │     Storage Layer                                        │ │
│  │  ┌─────────────────────────────────────────────────────┐ │
│  │  │  utils/storage.ts                                    │ │
│  │  │  (localStorage Functions)                            │ │
│  │  │  - registerUser()                                    │
│  │  │  - saveUserData()                                    │
│  │  │  - saveAssessmentResult()                            │
│  │  │  - saveAiInsights()                                  │
│  │  │  - etc.                                              │
│  │  └─────────────┬──────────────────────────────────────┘ │
│  │                │                                         │
│  │  ┌─────────────▼──────────────────────────────────────┐ │
│  │  │  utils/server-sync.ts                               │
│  │  │  (Server Sync Functions)                            │
│  │  │  - syncUserData()                                   │
│  │  │  - fetchUserData()                                  │
│  │  │  - checkServerHealth()                              │
│  │  │  - etc.                                              │
│  │  └─────────────┬──────────────────────────────────────┘ │
│  └────────────────┼───────────────────────────────────────┘ │
│                   │ HTTP Requests/Responses                  │
└───────────────────┼──────────────────────────────────────────┘
                    │ (Port 5173 ←→ Port 3001)
                    │
    ┌───────────────▼────────────────────┐
    │   localStorage / sessionStorage    │
    │   (Browser DB)                     │
    └───────────────────────────────────┘
                    │
                    │ (when online)
                    │
┌───────────────────▼────────────────────────────────────────┐
│              Node.js Server (Express.js)                   │
│              (http://localhost:3001)                       │
├───────────────────────────────────────────────────────────┤
│                                                             │
│  API Routes (/api)                                        │
│  ┌─────────────────────────────────────────────────────┐ │
│  │  /auth/register    POST                              │ │
│  │  /auth/login       POST                              │ │
│  │  /users            GET                               │ │
│  └─────────────────────────────────────────────────────┘ │
│  ┌─────────────────────────────────────────────────────┐ │
│  │  /users/:userId/knowledge-bases     GET/POST        │ │
│  │  /users/:userId/assessment-results  GET/POST        │ │
│  │  /users/:userId/assessment-history  GET/POST        │ │
│  │  /users/:userId/ai-insights         GET/POST        │ │
│  └─────────────────────────────────────────────────────┘ │
│  ┌─────────────────────────────────────────────────────┐ │
│  │  /model-config     GET/POST                         │ │
│  │  /prompts-config   GET/POST                         │ │
│  │  /backup           POST                             │ │
│  │  /restore          POST                             │ │
│  │  /health           GET                              │ │
│  └─────────────────────────────────────────────────────┘ │
│                                    │                       │
│                    ┌───────────────▼──────────────────┐   │
│                    │  File I/O                        │   │
│                    │  (fs module)                     │   │
│                    └───────────────┬──────────────────┘   │
└────────────────────────────────────┼────────────────────┘
                                    │
                                    │
                    ┌───────────────▼──────────────────┐
                    │   File System (JSON Storage)     │
                    │   server/storage/                │
                    │                                  │
                    │   users.json                     │
                    │   model_config.json              │
                    │   prompts_config.json            │
                    │                                  │
                    │   user-123/                      │
                    │   ├── knowledge_bases.json       │
                    │   ├── assessment_results.json    │
                    │   ├── assessment_history.json    │
                    │   └── ai_insights.json           │
                    │                                  │
                    │   user-456/                      │
                    │   └── ...                        │
                    └──────────────────────────────────┘
```

## 数据流向 (Data Flow)

### 场景 1: 保存知识库 (Save Knowledge Base)

```
User Action (添加知识库)
    ↓
React Component
    ↓
handleSaveKB(userId, kb)
    ├─→ saveUserData(userId, kbList)        [localStorage]
    │   └─→ browser LocalStorage updated
    │
    └─→ syncUserData(userId, kbList)        [Server Sync]
        └─→ HTTP POST /api/users/:userId/knowledge-bases
            ├─→ Express Router
            ├─→ writeJsonFile() [fs module]
            └─→ File saved: server/storage/user-123/knowledge_bases.json
```

### 场景 2: 加载知识库 (Load Knowledge Base)

```
App Initialize (应用启动)
    ↓
handleLoadKB(userId)
    │
    ├─→ fetchUserData(userId)               [Server First]
    │   └─→ HTTP GET /api/users/:userId/knowledge-bases
    │       ├─→ readJsonFile()
    │       └─→ Return data from server
    │
    ├─→ (If server available)
    │   ├─→ saveUserData(userId, serverData) [Update localStorage]
    │   └─→ setKnowledgeBases(serverData)
    │
    └─→ (If server unavailable)
        └─→ getUserData(userId)              [Fallback to localStorage]
            └─→ setKnowledgeBases(localData)
```

### 场景 3: 测试评分 (Record Assessment)

```
User Completes Test
    ↓
handleSaveScore(userId, result)
    │
    ├─→ saveAssessmentResult(userId, result)     [localStorage]
    │
    ├─→ getAssessmentResults(userId)             [Get all results]
    │
    └─→ syncAssessmentResults(userId, allResults) [Server Sync]
        └─→ HTTP POST /api/users/:userId/assessment-results
            └─→ File saved: server/storage/user-123/assessment_results.json
```

## 混合存储策略 (Hybrid Storage Strategy)

```
┌─────────────────────────────────────────────────────────┐
│  应用启动 → 检查服务器                                    │
└────────────┬────────────────────────────────────────────┘
             │
      ┌──────┴──────┐
      │             │
   在线 (Online)   离线 (Offline)
      │             │
      ▼             ▼
  └─────────┐  ┌─────────┐
    使用     │  │  使用    
    Server  │  │localStorage
    + Local │  │   Only
    Storage │  │
  └─────────┘  └─────────┘
      │            │
      │  (当恢复)  │
      ▼    ▼
   ┌────────────┐
   │ 数据同步   │
   │(Upload)   │
   └────────────┘
```

## API 端点总览 (API Endpoints)

### 认证 (Authentication)
```
POST   /api/auth/register          注册新用户
POST   /api/auth/login              用户登录
GET    /api/users                  获取所有用户
```

### 知识库 (Knowledge Bases)
```
GET    /api/users/:userId/knowledge-bases
POST   /api/users/:userId/knowledge-bases
```

### 测试成绩 (Assessment Results)
```
GET    /api/users/:userId/assessment-results
POST   /api/users/:userId/assessment-results
```

### 学习历史 (Assessment History)
```
GET    /api/users/:userId/assessment-history
POST   /api/users/:userId/assessment-history
```

### AI 洞察 (AI Insights)
```
GET    /api/users/:userId/ai-insights
POST   /api/users/:userId/ai-insights
```

### 配置 (Configuration)
```
GET    /api/model-config
POST   /api/model-config
GET    /api/prompts-config
POST   /api/prompts-config
```

### 备份与恢复 (Backup & Restore)
```
POST   /api/backup                 创建完整备份
POST   /api/restore                恢复备份
```

### 健康检查 (Health Check)
```
GET    /api/health                 检查服务器状态
```

## 文件结构 (File Structure)

```
learn-copilot/
│
├── 📁 src/
│   ├── App.tsx                  主应用组件
│   ├── index.tsx                入口点
│   ├── types.ts                 TypeScript 定义
│   │
│   ├── 📁 components/
│   │   └── Sidebar.tsx          侧边栏
│   │
│   ├── 📁 pages/                页面组件
│   │   ├── Auth.tsx             认证页面
│   │   ├── Library.tsx          资料库
│   │   ├── Graph.tsx            知识图谱
│   │   ├── Practice.tsx         练习模式
│   │   ├── Assessment.tsx       智能测试
│   │   ├── Analytics.tsx        学习分析
│   │   ├── LearningPath.tsx     学习路径
│   │   ├── About.tsx            关于
│   │   └── Settings.tsx         设置
│   │
│   └── 📁 utils/
│       ├── storage.ts           ⭐ localStorage 函数
│       ├── server-sync.ts       ⭐ 服务器同步函数
│       ├── ai.ts                AI 相关函数
│       └── prompts.ts           提示词管理
│
├── 📁 server/                   ⭐ 后端服务器
│   ├── index.js                 Express 服务器
│   ├── 📁 services/             (可扩展)
│   └── 📁 storage/              ⭐ JSON 数据文件
│       ├── users.json
│       ├── model_config.json
│       ├── prompts_config.json
│       └── user-{userId}/
│
├── 📁 (配置文件)
│   ├── package.json             ⭐ 已更新
│   ├── tsconfig.json
│   ├── vite.config.ts
│   └── .env.example             ⭐ 环境变量
│
└── 📁 (文档)
    ├── QUICKSTART.md            ⭐ 快速开始
    ├── SERVER_SETUP.md          ⭐ API 文档
    ├── DATA_STORAGE.md          ⭐ 数据说明
    ├── MIGRATION_GUIDE.md       ⭐ 迁移指南
    ├── SYNC_EXAMPLES.ts         ⭐ 代码示例
    └── PROJECT_SUMMARY.md       ⭐ 项目总结
```

## 技术栈 (Tech Stack)

| 层 | 技术 | 版本 |
|---|------|------|
| Frontend Framework | React | 19.2.3 |
| Build Tool | Vite | 6.2.0 |
| Language | TypeScript | ~5.8.2 |
| Backend | Express.js | ^4.18.2 |
| Runtime | Node.js | Latest |
| CORS | cors | ^2.8.5 |
| AI | Google GenAI | ^1.37.0 |
| Storage | JSON Files | Built-in |

## 状态管理 (State Management)

```
Frontend State:
├── LocalStorage (Browser)
│   ├── Users
│   ├── Knowledge Bases
│   ├── Assessment Results
│   ├── Assessment History
│   ├── AI Insights
│   ├── Model Config
│   └── Prompts Config
│
├── Memory (React State)
│   └── UI State (selected KB, current user, etc.)
│
Server State:
└── File System (server/storage/)
    └── All persistent data (JSON files)
```

## 错误处理流程 (Error Handling Flow)

```
API Request
    ↓
Success?
    ├─→ YES → Process Data
    │         Update UI
    │
    └─→ NO  → Catch Error
              Log Error
              Show User Message
              (For sync operations)
              ↓
              Fallback to localStorage
```

## 性能优化建议 (Performance Optimization)

1. **缓存** - 使用 Redis 缓存频繁访问的数据
2. **分页** - 长列表使用分页加载
3. **压缩** - 使用 gzip 压缩 API 响应
4. **数据库** - 从 JSON 文件迁移到数据库
5. **索引** - 在频繁查询的字段上添加索引
6. **CDN** - 静态资源使用 CDN

## 安全建议 (Security Recommendations)

1. **HTTPS** - 生产环境使用 HTTPS
2. **认证** - 实现 JWT tokens
3. **授权** - 验证用户权限
4. **验证** - 验证所有输入
5. **密码** - 使用 bcrypt 哈希
6. **日志** - 记录敏感操作
7. **备份** - 定期备份数据
8. **加密** - 加密敏感数据

---

**更新时间**: 2026-01-19
**架构版本**: 1.0
